import { PlaceholderShell } from "@/components/shared";

export const dynamic = "force-dynamic";

export default function SettingsPage() {
  return (
    <PlaceholderShell
      title="Settings"
      description="Model / Browser / Runtime 三段配置 —— 第五阶段实现"
    />
  );
}
