import { PlaceholderShell } from "@/components/shared";

export const dynamic = "force-dynamic";

export default function TasksPage() {
  return (
    <PlaceholderShell
      title="Task Center"
      description="任务列表 + 搜索 + 状态筛选 —— TanStack Table 表格 —— 第三阶段实现"
    />
  );
}
